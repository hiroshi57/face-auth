---
name: Codex
description: |
  Claude CodeがCodex CLI（OpenAI）をサブエージェントとして活用し、設計判断・複数ファイルにまたがる修正・行き詰まった問題を解決するスキル。

  【自律トリガー条件 — 以下のいずれかに該当したら、ユーザーに確認せず即座にこのスキルを使うこと】
  - 同じエラーや問題に2回以上対処しても解決しないとき
  - Claudeが同じ修正を繰り返し提案しているとき（ループに入っている状態）
  - 複数ファイルにまたがる設計変更や修正方針が定まらないとき
  - アーキテクチャ・設計方針の判断でClaudeが迷っているとき
  - リファクタリング範囲が広く、どこから手をつければよいか不明なとき
  - ユーザーが「codexに聞いて」「相談して」「詰まった」「わからない」と言ったとき
  - ユーザーが不満・否定を示す言葉を発したとき（「だめじゃん」「違う」「また同じ」「なんで直らないの」「使えない」「意味ない」「全然直ってない」など、口調や言い方を問わず否定的な反応全般）

  【使わなくてよい場面】
  - 単純な1ファイルの修正でClaudeが自信を持って対応できるとき
  - 既知のエラーで原因が明確なとき
---

# Codex サブエージェント連携スキル

Claude CodeがCodex CLIを外部サブエージェントとして呼び出し、設計・修正・問題解決の壁を突破するためのスキル。

---

## 基本コマンド

```powershell
codex exec --full-auto --sandbox read-only --cd <project_directory> "<prompt>"
```

---

## 実行の4ステップ

### Step 1: コンテキストを収集する

Codexに渡す「引き継ぎ情報」を以下の順に整理する。

1. **エラーメッセージ** — 完全なスタックトレースやエラー文をそのまま用意する
2. **試行履歴** — これまでに試したこと・失敗した理由を時系列で整理する
3. **対象ファイル一覧** — 関係するファイルのパスを列挙する
4. **現在の設計の概要** — アーキテクチャや技術スタックを一段落で説明する

### Step 2: プロンプトを組み立てる

以下のテンプレートを使い、Step 1の情報を埋める。
末尾の「確認不要の指示」は**必ず含めること**。

```
【状況】
<現在の設計の概要を1〜3文で>

【問題・エラー】
<エラーメッセージまたは詰まっている内容をそのまま貼る>

【これまでに試したこと】
1. <試したこと> → <結果・なぜ失敗したか>
2. <試したこと> → <結果・なぜ失敗したか>
...

【依頼】
<修正案の提示 / 設計方針の判断 / リファクタリング計画 など>

確認や質問は不要です。修正案・改善案・コード例まで自主的に出力してください。
```

### Step 3: Codexを実行する

```powershell
codex exec --full-auto --sandbox read-only --cd <project_directory> "<Step2で組み立てたプロンプト>"
```

**ディレクトリの特定方法:**
- ユーザーがパスを指定していればそれを使う
- 指定がなければ現在のワーキングディレクトリを使う
- 複数プロジェクトにまたがる場合はルートディレクトリを指定する

### Step 4: 結果をユーザーに報告する

Codexの出力を受け取ったあと、以下の形式でユーザーに提示する。

```
【Codexの判断】
<設計方針や根本原因の要約 — 1〜3文>

【提案された修正案】
<コード例や修正手順をそのまま提示>

【改善案（あれば）】
<リファクタリングや設計改善の提案>

【Claudeの見解】
<Codexの提案に対してClaudeが同意するか、懸念点があれば一言添える>
```

---

## 用途別プロンプト例

### 設計方針で詰まったとき
```powershell
codex exec --full-auto --sandbox read-only --cd /path/to/project "【状況】Reactアプリで状態管理をContextとReduxのどちらで実装するか判断できていない。【問題】コンポーネント数が増えてprop drillingが深くなってきた。【試したこと】1. Contextだけで実装 → 再レンダリングが多発してパフォーマンスが悪化 / 2. Reduxに移行しようとしたが設計コストが高い。【依頼】このプロジェクト規模に適した状態管理の方針と移行手順を提案してください。確認や質問は不要です。具体的な実装例まで自主的に出力してください。"
```

### 複数ファイルにまたがる修正が必要なとき
```powershell
codex exec --full-auto --sandbox read-only --cd /path/to/project "【状況】認証処理をJWT方式からセッション方式に変更したい。【問題】auth.js, middleware.js, routes/user.js, routes/admin.js, tests/auth.test.js の5ファイルに影響が出る。どの順番で何を変えるべきかわからない。【試したこと】1. auth.jsだけ先に変えた → middlewareがJWTを期待していてエラー多発。【依頼】変更順序と各ファイルの修正内容を整理した移行計画を出してください。確認や質問は不要です。コード例まで自主的に出力してください。"
```

### エラーが取れないとき
```powershell
codex exec --full-auto --sandbox read-only --cd /path/to/project "【状況】Next.js 14のApp RouterでSSRを使っている。【問題・エラー】TypeError: Cannot read properties of undefined (reading 'map') — app/dashboard/page.tsx 23行目。【試したこと】1. オプショナルチェーン追加 → 同じエラー / 2. データのnullチェック追加 → 別の箇所でエラー。【依頼】根本原因を特定して修正案を出してください。確認や質問は不要です。修正コードまで自主的に出力してください。"
```

---

## 注意事項

- `--sandbox read-only` のため、Codexはファイルを直接書き換えない。あくまで「修正案の提示」止まり。実際の変更はClaudeまたはユーザーが行う。
- Codexの提案が複数案ある場合、Claudeはプロジェクトの文脈を踏まえてどれが最適かを判断してからユーザーに提示する。
- Codexが日本語で回答しない場合でも、Claudeが日本語に翻訳して報告する。
